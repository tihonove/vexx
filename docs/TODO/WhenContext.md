# Система контекста (when)

## Цель

Реализовать систему контекстных условий по аналогии с VS Code `when` clause — определяет, когда команда/кейбиндинг/пункт меню активен.

## Подзадачи

### [ ] 1. Спроектировать и реализовать ContextKeyService

Хранилище ключ→значение, вложенные скоупы (глобальный → виджет).

### [ ] 2. Парсинг и вычисление when-выражений

Выражения вида `editorFocus && !editorReadonly`.

### [ ] 3. Интегрировать с KeybindingRegistry и меню
